// If a spherical panorama output is used.
sphereView = true

// Output window size in pixels.
windowWidth = 1920
windowHeight = 1080

//Intermediate texture size in pixels
texWidth = 4096
texHeight = 2160

// Viewer settings
viewAngle = 0.5 //*PI
offsetX = 0 // *PI
offsetY = 0 // *PI

//Frame settings if not movement mode 1 they are automatically overwritten to 1
nrOfFrames = 10
nrOfGrids = 2

n_black_hole_angles = 50;

//Effect settings
useRedshift = true
useLensing = false
useStars = false

//AccretionDisk
useAccretionDisk = true
//Minimum and maximum accretion disk radius 6 is the minimum stable orbit around the black hole and lower values for the minimum will cause issues
accretionDiskMinRadius = 6.
accretionDiskMaxRadius = 40.
temperatureLUTSize = 500
blackholeMass = 3.5e9 //Mass of the black hole in solar masses
blackholeAccretionRate = 0.1 //The accretion rate of the black hole in solar masses per year

useAccretionDiskTexture = false
accretionDiskTexture = "AcretionTest.png"

//AcretionDiskInterpolation
n_disk_angles = 200;
n_disk_sample = 200;
max_disk_segments = 4;

//Option to save the geodesics used to generate the image
saveGeodesics = false

// Image location.
celestialSkyImg = "rainbow.png"
// Star file location.
starCatalogue = "sterren.txt"
// Star binary tree depth
starTreeLevel = 8
// Stars only with magnitude Higher than
starMagnitudeCutLow = 1.
// Stars only with magnitude Lower than
starMagnitudeCutHigh = 1000.
// Randomly throw away stars at start to increase performance
randomStarSelection = true
// chance to keep star when using random selection
randomSelectionChance = 0.1
// Star diffraction img
diffractionImg = "0s1.png"

// Grid Settings
gridStartLevel = 1
gridMinLevel = 2
gridMaxLevel = 11

// Blackhole angular velocity
afactor = 0.982;

// Movement mode determines what should happen
// 0 = single frame
// 1 = Move along path
// 2 = Free movement
movementMode = 2

// Camera (starting) location, speed, direction
cameraFov = 70.

camRadius = 50.
camInclination = 0.4 //*PI
camPhi = 0. //*PI
camSpeed = 0.32001

// If a custom user speed is used.
userSpeed = false

// Camera Direction Vector
br = 0.
bphi = 1.
btheta = 0.

// if movement mode 1 the final value of the parameters
camSpeedChange = false
camSpeedTo = 0.0001

camRadiusChange = false
camRadiusTo = 30.

camInclinationChange = true
camInclinationTo = 0.45 //*PI

camPhiChange = false
camPhiTo = 0.5 //*PI

//If movement mode 2 the distance before we generate a new grid
gridDistanceTheta = 0.1 //*PI
gridDistanceR = 5.0
